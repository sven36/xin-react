// const URL_PREFIX = process.env.NODE_ENV === 'development' ?
//     `//${window.location.host}` :
//     `//${window.location.host}`;
const URLCONFIG={
    testUrl:'/test/aaa',
};
export {
    // URL_PREFIX,
    URLCONFIG
}